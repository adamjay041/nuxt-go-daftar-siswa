import Vue from 'vue'
import Router from 'vue-router'
import { normalizeURL, decode } from 'ufo'
import { interopDefault } from './utils'
import scrollBehavior from './router.scrollBehavior.js'

const _2941df6d = () => interopDefault(import('../../../pages/login.vue' /* webpackChunkName: "pages/login" */))
const _662ac0b0 = () => interopDefault(import('../../../pages/siswa.add.x.vue' /* webpackChunkName: "pages/siswa.add.x" */))
const _b4d6b2b2 = () => interopDefault(import('../../../pages/siswa.edit.x.vue' /* webpackChunkName: "pages/siswa.edit.x" */))
const _5f9a3c56 = () => interopDefault(import('../../../pages/index.vue' /* webpackChunkName: "pages/index" */))

const emptyFn = () => {}

Vue.use(Router)

export const routerOptions = {
  mode: 'history',
  base: '/',
  linkActiveClass: 'nuxt-link-active',
  linkExactActiveClass: 'nuxt-link-exact-active',
  scrollBehavior,

  routes: [{
    path: "/login",
    component: _2941df6d,
    name: "login"
  }, {
    path: "/siswa.add.x",
    component: _662ac0b0,
    name: "siswa.add.x"
  }, {
    path: "/siswa.edit.x",
    component: _b4d6b2b2,
    name: "siswa.edit.x"
  }, {
    path: "/",
    component: _5f9a3c56,
    name: "index"
  }],

  fallback: false
}

export function createRouter (ssrContext, config) {
  const base = (config._app && config._app.basePath) || routerOptions.base
  const router = new Router({ ...routerOptions, base  })

  // TODO: remove in Nuxt 3
  const originalPush = router.push
  router.push = function push (location, onComplete = emptyFn, onAbort) {
    return originalPush.call(this, location, onComplete, onAbort)
  }

  const resolve = router.resolve.bind(router)
  router.resolve = (to, current, append) => {
    if (typeof to === 'string') {
      to = normalizeURL(to)
    }
    return resolve(to, current, append)
  }

  return router
}
